-content_for :block_title, 'Usuários'
-content_for :sidebar, render(:partial => 'sidebar')

%table.table
  %tr
    %th.first ID
    %th=t 'activerecord.attributes.user.name'
    %th=t 'activerecord.attributes.user.email'
    %th=t 'web-app-theme.created_at'
    %th.last &nbsp;
  -@users.each do |user|
    %tr{:class => cycle('odd', 'even')}
      %td=user.id
      %td=user.name
      %td=user.email
      %td=l user.created_at
      %td.last
        =link_to t('web-app-theme.edit'), edit_user_registration_path(user)
        -# |
        -# =link_to t('web-app-theme.delete'), user_path(user)', :method => :delete, :confirm => '#{t('web-app-theme.confirm', :default => 'Are you sure?')}'}

.actions-bar.wat-cf
  .actions
